import { verifyToken } from '@/actions/verification-token.action';

const VerifyEmailPage = async () => {
  // const verify = await verifyToken();
  return <div>verify</div>;
};

export default VerifyEmailPage;
